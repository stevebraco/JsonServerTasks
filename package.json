{
  "name": "jsonback-server",
  "version": "1.0.0",
  "description": "JSON server",
  "main": "index.js",
  "homepage": "https://stevebraco.github.io/JsonServerTasks",

  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "npx json-server --watch src/db.json --port=8000",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
    
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
